= content_for :javascript do
  = javascript_include_tag 'sympathizers'

%h3
  Asignar simpatizante
  .pull-right
    = link_to 'Lista de simpatizantes', admin_sympathizers_path, class: 'btn btn-default pull-right'

= render partial: 'form'